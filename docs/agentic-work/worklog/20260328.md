# 2026-03-28 Worklog

## Day Goal

- Implement `todo.md` item `B-1`.
- Replace the current image-level-only manifest view with a lesion-level bag
  protocol that later `B-2`, `B-3`, and `B-4` work can consume directly.
- Write protocol docs and keep repository guidance aligned with the new command.

## Work Completed

### 1. Added A Dedicated Bag Protocol Builder

- Created `src/build_bag_protocol.py` as a standalone CLI for `B-1`.
- Added `src/pancreas_vision/bag_protocol.py` to build lesion-level bag rows,
  instance rows, QC summaries, and review candidates without changing the
  existing training entrypoints.

### 2. Locked The Formal B-1 Scope

- Included only the current main-line buckets:
  - `caerulein_adm`
  - `KC`
  - `KPC`
  - `multichannel_adm`
  - `multichannel_kc_adm`
- Kept `multichannel_unresolved` outside the formal manifests.
- Still used `data/2.csv` metadata from `multichannel_unresolved` to populate
  `review_candidates.csv` so unresolved rows remain visible for manual triage.

### 3. Generated The First Bag Protocol Artifacts

- Ran `PYTHONPATH=src python3 src/build_bag_protocol.py`
- Wrote outputs under `artifacts/bag_protocol_v1/`:
  - `instance_manifest.csv`
  - `bag_manifest.csv`
  - `review_candidates.csv`
  - `summary.json`
  - `summary.md`

### 4. Verified Key Contract Properties

- `instance_manifest.csv` uses `split_key == bag_id` for every row.
- `bag_manifest.csv` instance counts match the instance manifest aggregation.
- KC ROI polygons are preserved as `roi_crop` instances with `is_roi = 1`.
- `LESION_KPC_001` correctly groups:
  - `data/KPC/1.tif`
  - `data/KPC/1-amylase.tif`
  - `data/KPC/1-ck-19.tif`
- `data/KPC/KC-40x-merge.tif` is surfaced as a
  `filename_bucket_conflict` review item.

### 5. Main Findings From The Output

- Formal protocol size:
  - `35` bags
  - `80` instances
  - `27` ROI instances
- Review candidate count is `45`, driven mainly by:
  - many `single_view_bag` cases
  - many `missing_magnification` cases
  - one cross-bucket grouped lesion: `LESION_MC_001`
- This confirms the repository now has a concrete bag contract, but also makes
  the main metadata debt explicit instead of hiding it in filenames.

## Files Added Or Updated

- `src/build_bag_protocol.py`
- `src/pancreas_vision/bag_protocol.py`
- `artifacts/bag_protocol_v1/`
- `docs/agentic-work/BAG_PROTOCOL_B1_2026-03-28.md`
- `docs/agentic-work/worklog/20260328.md`

## Next Dependencies Unblocked

- `B-2`: grouped split can consume `split_key == bag_id`
- `B-3`: feature caching can iterate over `instance_manifest.csv`
- `A-1` and `A-2`: manual review can focus on `review_candidates.csv` instead of
  re-scanning the full dataset
