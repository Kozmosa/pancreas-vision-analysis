# 2026-03-28 Worklog B-2

## Day Goal

- Implement `todo.md` item `B-2`.
- Freeze lesion-level split files based on the existing bag protocol.
- Publish a reusable evaluation template so later experiments report results in
  the same bag-level structure.

## Work Completed

### 1. Added A Dedicated Split Protocol Builder

- Created `src/build_split_protocol.py`
- Created `src/pancreas_vision/split_protocol.py`

The new builder consumes `artifacts/bag_protocol_v1/bag_manifest.csv` and
generates the fixed split artifacts required for later grouped experiments.

### 2. Froze The Main Lesion-Level Split

- Split unit is now `bag_id`
- Seed is fixed to `42`
- Ratio is fixed to `7:3`
- Main split is stratified on bag-level `label_name`

Generated result:

- train: `24` bags
- test: `11` bags
- label counts:
  - train `ADM=10`, `PanIN=14`
  - test `ADM=4`, `PanIN=7`

### 3. Added Grouped 5-Fold CV

- Added `grouped_5fold.csv`
- Used stratified 5-fold assignment on bag labels
- Verified that each fold has `28` train bags and `7` test bags
- Verified zero `bag_id` overlap between train and test inside every fold

### 4. Froze The Evaluation Template

- Added `evaluation_template.json`
- Required outputs are now:
  - `bag_level_metrics`
  - `bag_level_predictions`
  - `error_by_source_bucket`

This gives later `B-3/B-4/B-5` work one fixed target format instead of
experiment-specific ad hoc summaries.

### 5. Wrote Formal Artifacts

Generated under `artifacts/split_protocol_v1/`:

- `main_split.csv`
- `grouped_5fold.csv`
- `summary.json`
- `summary.md`
- `evaluation_template.json`

## Verification

- `python3 -m compileall src` passed
- `PYTHONPATH=src python3 src/build_split_protocol.py --help` passed
- `PYTHONPATH=src python3 src/build_split_protocol.py` generated the full output
- Post-run validation confirmed:
  - `35` rows in `main_split.csv`
  - `175` rows in `grouped_5fold.csv`
  - zero overlap between fold train/test bag sets

## Files Added Or Updated

- `src/build_split_protocol.py`
- `src/pancreas_vision/split_protocol.py`
- `artifacts/split_protocol_v1/`
- `docs/agentic-work/SPLIT_PROTOCOL_B2_2026-03-28.md`
- `docs/agentic-work/worklog/20260328_b2.md`

## Next Dependencies Unblocked

- `B-3` can cache features against a fixed train/test or fold assignment
- `B-4` can report results on a stable lesion-level evaluation protocol
- later comparisons can stop relying on one-off image-level splits
